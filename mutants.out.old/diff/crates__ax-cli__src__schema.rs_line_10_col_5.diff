--- crates/ax-cli/src/schema.rs
+++ replace envelope_schema -> String with String::new()
@@ -2,99 +2,17 @@
 //!
 //! A JSON Schema (draft 2020-12) pinning the `tq1` envelope. Agents validate
 //! against this instead of reverse-engineering field names. If the envelope
 //! shape changes, this schema (and the contract tests over it) must change too.
 
 use ax_core::envelope::{FINDING_COLUMNS, PROTOCOL};
 
 pub fn envelope_schema() -> String {
-    let schema = serde_json::json!({
-        "$schema": "https://json-schema.org/draft/2020-12/schema",
-        "$id": "https://anomalyx/tq1/envelope.schema.json",
-        "title": "anomalyx tq1 envelope",
-        "type": "object",
-        "required": [
-            "protocol", "config_version", "source", "format",
-            "rows_scanned", "dict", "columns", "rows", "absent", "summary", "exit"
-        ],
-        "additionalProperties": false,
-        "properties": {
-            "protocol": { "const": PROTOCOL },
-            "config_version": { "type": "string" },
-            "source": { "type": "string" },
-            "format": { "type": "string" },
-            "rows_scanned": { "type": "integer", "minimum": 0 },
-            "dict": {
-                "type": "array",
-                "items": { "type": "string" },
-                "description": "Dictionary-pinned string table; *_idx values index into this."
-            },
-            "columns": {
-                "type": "array",
-                "items": { "type": "string" },
-                "description": "Names of the dense finding-row columns.",
-                "const": FINDING_COLUMNS
-            },
-            "rows": {
-                "type": "array",
-                "description": "Dense finding rows, each aligned to `columns`.",
-                "items": {
-                    "type": "array",
-                    "minItems": 7,
-                    "maxItems": 7,
-                    "prefixItems": [
-                        { "type": "integer", "description": "detector dict index" },
-                        { "type": "integer", "description": "class dict index" },
-                        { "type": "integer", "description": "handle dict index" },
-                        { "type": "number", "description": "confidence in [0,1]" },
-                        { "type": "integer", "description": "severity dict index" },
-                        { "type": "number", "description": "raw detector score" },
-                        { "type": "integer", "description": "reason dict index" }
-                    ]
-                }
-            },
-            "absent": {
-                "type": "array",
-                "description": "Detectors that declined to run, with reasons (honest absence).",
-                "items": {
-                    "type": "object",
-                    "required": ["detector", "reason"],
-                    "properties": {
-                        "detector": { "type": "string" },
-                        "reason": { "type": "string" }
-                    }
-                }
-            },
-            "summary": {
-                "type": "object",
-                "required": ["total", "by_class"],
-                "properties": {
-                    "total": { "type": "integer", "minimum": 0 },
-                    "max_severity": {
-                        "type": ["string", "null"],
-                        "enum": ["info", "low", "medium", "high", "critical", null]
-                    },
-                    "by_class": {
-                        "type": "array",
-                        "items": {
-                            "type": "object",
-                            "required": ["class", "count"],
-                            "properties": {
-                                "class": { "type": "string" },
-                                "count": { "type": "integer", "minimum": 0 }
-                            }
-                        }
-                    }
-                }
-            },
-            "exit": { "type": "integer", "enum": [0, 1, 2] }
-        }
-    });
-    serde_json::to_string_pretty(&schema).expect("schema serializes")
+    String::new() /* ~ changed by cargo-mutants ~ */
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
 
     #[test]
     fn schema_is_valid_json_and_pins_protocol() {
