[package]
name = "ax-normalize"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "anomalyx normalization: any input format -> the ax-core RecordSet"

[dependencies]
ax-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
csv = "1"
# Polars/Arrow backbone for binary columnar formats lands behind the RecordSet
# boundary in the next slice (Parquet, Arrow IPC) — text formats are handled
# here with a fully-deterministic reader.

[dev-dependencies]
proptest = { workspace = true }
