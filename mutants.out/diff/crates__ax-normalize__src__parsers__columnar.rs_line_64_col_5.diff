--- crates/ax-normalize/src/parsers/columnar.rs
+++ replace parse_err -> AxError with Default::default()
@@ -56,20 +56,17 @@
     if f.is_finite() {
         Value::Float(f)
     } else {
         Value::Null
     }
 }
 
 fn parse_err(id: &str, e: impl std::fmt::Display) -> AxError {
-    AxError::Parse {
-        format: id.to_string(),
-        message: e.to_string(),
-    }
+    Default::default() /* ~ changed by cargo-mutants ~ */
 }
 
 #[derive(Debug, Default, Clone)]
 pub struct ParquetParser;
 
 impl FormatParser for ParquetParser {
     fn id(&self) -> &'static str {
         "parquet"
