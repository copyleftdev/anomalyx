--- crates/ax-normalize/src/parsers/zeek.rs
+++ delete match arm "empty_field" in <impl FormatParser for ZeekParser>::parse
@@ -73,21 +73,17 @@
                         cols = vec![Vec::new(); names.len()];
                         fields = Some(names);
                     }
                     "unset_field" => {
                         if let Some(v) = parts.next() {
                             unset = v.to_string();
                         }
                     }
-                    "empty_field" => {
-                        if let Some(v) = parts.next() {
-                            empty = v.to_string();
-                        }
-                    }
+                     /* ~ changed by cargo-mutants ~ */
                     _ => {} // set_separator, types, path, open, close — ignored
                 }
                 continue;
             }
 
             // A data row. It must come after the `#fields` header.
             if fields.is_none() {
                 return Err(self.err("data row before #fields header"));
