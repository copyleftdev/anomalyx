--- crates/ax-normalize/src/table.rs
+++ replace TableBuilder::finish -> Vec<Column> with vec![Default::default()]
@@ -57,21 +57,17 @@
         for (name, &i) in &self.index {
             let cell = row.remove(name).unwrap_or(Value::Null);
             self.cols[i].push(cell);
         }
         self.rows += 1;
     }
 
     pub fn finish(self) -> Vec<Column> {
-        self.order
-            .into_iter()
-            .zip(self.cols)
-            .map(|(name, cells)| Column::new(name, cells))
-            .collect()
+        vec![Default::default()] /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
 
     #[test]
