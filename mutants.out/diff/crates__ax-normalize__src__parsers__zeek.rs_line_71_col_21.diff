--- crates/ax-normalize/src/parsers/zeek.rs
+++ delete match arm "fields" in <impl FormatParser for ZeekParser>::parse
@@ -63,21 +63,17 @@
                 if let Some(val) = rest.strip_prefix("separator") {
                     if let Some(c) = decode_separator(val.trim()) {
                         sep = c;
                     }
                     continue;
                 }
                 let mut parts = rest.split(sep);
                 match parts.next().unwrap_or("") {
-                    "fields" => {
-                        let names: Vec<String> = parts.map(str::to_string).collect();
-                        cols = vec![Vec::new(); names.len()];
-                        fields = Some(names);
-                    }
+                     /* ~ changed by cargo-mutants ~ */
                     "unset_field" => {
                         if let Some(v) = parts.next() {
                             unset = v.to_string();
                         }
                     }
                     "empty_field" => {
                         if let Some(v) = parts.next() {
                             empty = v.to_string();
